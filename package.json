{
  "name": "homesfy-chat-buddy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "setup": "node setup.js",
    "dev": "concurrently -n \"API,DASHBOARD,WIDGET\" -c \"blue,green,yellow\" \"npm run dev:api\" \"npm run dev:dashboard\" \"npm run dev:widget\"",
    "start": "npm run dev",
    "dev:api": "cd apps/api && npm run dev",
    "dev:dashboard": "cd apps/dashboard && npm run dev",
    "dev:widget": "cd apps/widget && npm run dev",
    "install:all": "npm install && cd apps/api && npm install && cd ../dashboard && npm install && cd ../widget && npm install && cd ../..",
    "build": "npm run build:dashboard && npm run build:widget",
    "build:dashboard": "cd apps/dashboard && npm run build",
    "build:widget": "cd apps/widget && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
